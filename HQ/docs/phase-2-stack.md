# Phase 2 Tech Stack — Dynamic Sites

**Read this when:** ZeroEn crosses 15+ paying clients and Phase 2 opens. Until then, Phase 1 (landing pages) is the only supported flow.

| Layer | Technology |
|-------|-----------|
| Backend/DB | Supabase (Auth, Database, Storage) — free tier per client |
| Validation | Zod |
| + everything from Phase 1 | Next.js App Router, Tailwind, shadcn/ui, Vercel hosting |

**Gate:** Phase 2 is **not available** in Phase 1. Don't build dynamic sites (auth, database) for clients until the 15-client threshold is crossed and the operator explicitly promotes the project.
